exports.run = async (client, msg, params = []) => { // eslint-disable-line
	msg.delete();
};


exports.conf = {
	enabled: true,
	aliases: []
};


exports.help = {
	name: 'alive',
	shortdescription: '-',
	description: '-',
	usage: '-'
};
