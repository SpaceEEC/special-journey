exports.run = async (client, msg, params = []) => {
  msg.delete();
};


exports.conf = {
  enabled: true,
  aliases: [],
};


exports.help = {
  name: 'alive',
  shortdescription: '-',
  description: '-',
  usage: '-',
};
