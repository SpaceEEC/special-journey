exports.run = async (client, msg, params = []) => { // eslint-disable-line no-unused-vars
	msg.delete();
};


exports.conf = {
	enabled: true,
	aliases: [],
};


exports.help = {
	name: 'alive',
	shortdescription: '-',
	description: '-',
	usage: '-',
};
